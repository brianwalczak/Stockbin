<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <title><%= editing ? 'Editor' : 'Create' %> | Stockbin</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
    <link rel="icon" type="image/png" href="/favicon.png">
    <link rel="manifest" href="/manifest.json">
	
	<meta name="apple-mobile-web-app-capable" content="yes">
	<meta name="mobile-web-app-capable" content="yes">
</head>
<body class="bg-gray-100">
    <%- include('snippets/header') %>

	<section class="main">
		<div class="mx-4 my-4 max-w-md mx-auto">
			<h2 class="text-xl font-semibold"><%= editing ? 'Update ' + (editing.type === 'item' ? 'Item' : 'Bin') : 'Create New' %></h2>
		</div>
	
		<div class="mx-4 max-w-md mx-auto">
			<% if (!editing) { %>
				<div class="mx-4 max-w-md mx-auto">
					<div class="radio mx-4 max-w-md mx-auto">
						<label class="block text-sm font-medium text-gray-700 mb-1">Type</label>
						<div class="flex space-x-4">
							<label class="item flex items-center cursor-pointer p-2 rounded hover:bg-gray-200 bg-gray-300">
								<input type="radio" name="type" value="item" class="hidden" checked>
								<span class="material-icons text-3xl mr-2">inventory_2</span>
								<span class="text-sm">Item</span>
							</label>
							<label class="item flex items-center cursor-pointer p-2 rounded hover:bg-gray-200">
								<input type="radio" name="type" value="bin" class="hidden">
								<span class="material-icons text-3xl mr-2">inbox</span>
								<span class="text-sm">Bin</span>
							</label>
						</div>
					</div>
				</div>
			<% } %>

			<% if (!editing || editing.type === 'item') { %>
				<form class="item" action="<%= '/api/editor/' + (editing ? editing.id : '') %>" method="POST">
					<input type="hidden" name="type" value="item">

					<div class="mx-4 max-w-md mx-auto mt-4">
						<label for="name" class="block text-sm font-medium text-gray-700 mb-1">Item Name*</label>
						<input name="name" style="width: 100%;" type="text" class="w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm" placeholder="Enter item name" value="<%= editing ? editing.name : '' %>" required>
					</div>

					<div class="mx-4 max-w-md mx-auto mt-4">
						<label for="id" class="block text-sm font-medium text-gray-700 mb-1">Item ID / UPC*</label>
						<input name="id" style="width: 100%;" type="text" class="w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm" placeholder="xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx" value="<%= editing ? editing.id : crypto.randomUUID() %>" required>
						<p class="block text-sm font-medium text-gray-400 mb-1 mt-1">Enter a product barcode if available, otherwise use default.</p>
					</div>

					<div class="mx-4 max-w-md mx-auto mt-4">
						<label for="quantity" class="block text-sm font-medium text-gray-700 mb-1">Item Quantity*</label>
						<input name="quantity" style="width: 100%;" type="number" inputmode="numeric" class="w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm" placeholder="Enter item quantity" min="1" value="<%= editing ? editing.quantity : 1 %>" required>
					</div>

					<% const bins = data.items.filter(item => item.type === 'bin'); %>

					<div class="mx-4 max-w-md mx-auto mt-4">
						<label for="location" class="block text-sm font-medium text-gray-700 mb-1">Item Location (<span class="material-icons align-middle" style="font-size: 1rem; color: black;">inbox</span>)*</label>
						<select name="location" class="w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm" required>
							<% if (bins.length === 0) { %>
								<option disabled selected>No bins available</option>
							<% } else { %>
								<% bins.forEach((item, index) => { %>
									<option value="<%= item.id %>" <%= ((editing && editing.bin === item.id) || (index === 0)) ? "selected" : "" %>><%= item.name %></option>
								<% }); %>
							<% } %>
						</select>
					</div>

					<div class="mx-4 max-w-md mx-auto mt-4">
						<label for="description" class="block text-sm font-medium text-gray-700 mb-1">Item Reference</label>
						<input name="description" style="width: 100%;" type="text" class="w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm" placeholder="Type a short description here" value="<%= editing && editing.description && editing.description.length > 0 ? editing.description : '' %>">
					</div>
					
					<div class="mx-4 max-w-md mx-auto mt-4">
						<label for="tags" class="block text-sm font-medium text-gray-700 mb-1">Category/Tags</label>
						<input name="tags" style="width: 100%;" type="text" class="w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm" placeholder="Type category or tags here, separated by commas." value="<%= editing && editing.tags && editing.tags.length > 0 ? editing.tags.join(', ') : '' %>">
					</div>

					<div class="mx-4 max-w-md mx-auto mt-4">
						<label for="threshold" class="block text-sm font-medium text-gray-700 mb-1">Low Stock Threshold</label>
						<input name="threshold" style="width: 100%;" type="number" inputmode="numeric" class="w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm" min="1" placeholder="Set a low stock threshold for alerts on your dashboard" value="<%= editing && editing.threshold && editing.threshold.length > 0 ? editing.threshold : '' %>">
					</div>


					<button type="submit" class="btn-red font-bold" <%= bins.length === 0 ? "disabled" : "" %>><%= editing ? "Update" : "Create" %></button>
				</form>
			<% } %>

			<% if (!editing || editing.type === 'bin') { %>
				<form class="bin" action="<%= '/api/editor/' + (editing ? editing.id : '') %>" method="POST" style="<%= !editing ? 'display: none;' : '' %>">
					<input type="hidden" name="type" value="bin">

					<div class="mx-4 max-w-md mx-auto mt-4">
						<label for="name" class="block text-sm font-medium text-gray-700 mb-1">Bin Name*</label>
						<input name="name" style="width: 100%;" type="text" class="w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm" placeholder="Enter bin name" value="<%= editing ? editing.name : '' %>" required>
					</div>

					<div class="mx-4 max-w-md mx-auto mt-4">
						<label for="id" class="block text-sm font-medium text-gray-700 mb-1">Bin ID*</label>
						<input name="id" style="width: 100%;" type="text" class="w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm" placeholder="xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx" value="<%= editing ? editing.id : crypto.randomUUID() %>" required>
					</div>

					<div class="mx-4 max-w-md mx-auto mt-4">
						<label for="location" class="block text-sm font-medium text-gray-700 mb-1">Bin Location</label>
						<input name="location" style="width: 100%;" type="text" class="w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm" placeholder="Warehouse, Closet, etc." value="<%= editing && editing.location && editing.location.length > 0 ? editing.location : '' %>">
					</div>

					<div class="mx-4 max-w-md mx-auto mt-4">
						<label for="description" class="block text-sm font-medium text-gray-700 mb-1">Bin Description</label>
						<input name="description" style="width: 100%;" type="text" class="w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm" placeholder="Type a short description here" value="<%= editing && editing.description && editing.description.length > 0 ? editing.description : '' %>">
					</div>

					<button type="submit" class="btn-red font-bold"><%= editing ? "Update" : "Create" %></button>
				</form>
			<% } %>
			
			<p class="status text-center text-sm mt-2"></p>
		</div>
	</section>

	<script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
	<script>window.isEditing = <%= editing ? 'true' : 'false' %>;</script>
	<script src="/js/create.js"></script>
    <%- include('snippets/footer') %>
</body>
</html>